import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.greenpart.inventory',
  appName: 'Greenpart Inventory',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
